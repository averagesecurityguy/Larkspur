{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://github.com/averagesecurityguy/larkspur/schemas/agent_plan.json",
    "title": "AgentPlan",
    "description": "Larkspur agent plan schema",
    "type": "object",
    "properties": {
        "objective": {
            "type": "string",
            "description": "The objective the user is trying to obtain."
        },
        "agent": {
            "type": "string",
            "description": "The agent that will complete the objective"
        },
        "checklist": {
            "type": "array",
            "description": "A checklist of items that prove the objective was obtained.",
            "minItems": 1,
            "items": {
                "type": "string",
                "description": "A single check that should be performed to ensure the objective was obtained."
            }
        }
    },
    "required": ["objective", "agent", "checklist"],
    "additionalProperties": false    
}
